spring:
  application:
    name: config-client
  cloud:
    config:
#      uri: http://localhost:8770/
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server

  profiles:
    active: dev